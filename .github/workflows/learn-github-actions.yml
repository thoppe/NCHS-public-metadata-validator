name: download-data-json
on: [push]
jobs:
  download-file:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: wget https://data.cdc.gov/data.json
      - run: cat data.json